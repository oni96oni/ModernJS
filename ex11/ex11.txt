변수에 값을 재할당하면 메모리 공간의 주소가 바뀐다.
    원시 값이 변경되는것이 아니다.

원시 타입과 객체 타입을 구분하는 이유
    1. 원시 값 : 변경 불가능한 값
       참조 값 : 변경 가능한 값
    
    2. 원시 값 : 메모리에 실제 값이 저장
       참조 값 : 메모리에 참조 값이 저장

    3. 다른 변수에 할당시 원시값은 원본의 원시 값이 복사되어 전달, 참조값은 원본의 참조 값이 복사되어 전달

